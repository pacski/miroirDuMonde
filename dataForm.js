var form = document.getElementById('form')


form.addEventListener("submit", function (e) {


    var nom = form.elements.nom.value;
    var prenom = form.elements.prenom.value;
    var race = form.elements.race.value;
    var raceBis = form.elements.raceBis.value;
    var vocation = form.elements.vocation.value;
    var croyance = form.elements.croyance.value;
    var taille = form.elements.taille.value;
    var poid = form.elements.poid.value;
    var colorEye = form.elements.colorEye.value;
    var colorHair = form.elements.colorHair.value;
    var sexe = form.elements.sexe.value;
    var location = form.elements.location.value;
    var location = form.elements.location.value;
    var histoire = form.elements.histoire.value;
    var animalWay = form.elements.animalWay.value;
    var elementWay = form.elements.elementWay.value;

    alert("cou");
});

